# 設定完成！🎉

您的長庚醫科開發環境已設定完成。

## 隨時可用的命令

按下 `Ctrl+Shift+P` 開啟命令面板，搜尋「**長庚醫科**」即可找到：

- **長庚醫科: 檢測開發環境** — 重新檢查 Node.js、Git、.NET SDK
- **長庚醫科: 建立設定檔** — 重新建立或套用 Profile 設定
- **長庚醫科: 安裝推薦延伸模組** — 補裝遺漏的延伸模組

## 停用啟動檢測

如果不想每次啟動都自動檢測環境，可以在設定中搜尋 `cgmh.skipEnvironmentCheck` 並啟用。
