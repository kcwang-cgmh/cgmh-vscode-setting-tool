# 建立設定檔

您可以選擇：

- **建立新的 Profile** — 建立一個獨立的設定檔（建議命名為「長庚醫科」），不影響您現有的設定
- **套用到目前的 Profile** — 將標準設定直接套用到您目前使用的設定檔

> 💡 只會補上缺少的設定，不會覆蓋您已有的自訂設定。

包含的設定項目：
- GitHub Copilot 核心設定與 Chat 配置
- 編輯器與終端設定
- GitLens AI 模型設定
